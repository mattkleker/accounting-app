import {
  numberFormat
} from 'accounting/helpers/number-format';

module('NumberFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = numberFormat(42);
  ok(result);
});
